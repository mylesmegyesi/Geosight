<% content_for :body do %>
    
    <p>
        <h1><%= @tag.tag %></h1>
    </p>
    
    <table>
        <tr>
            <th></th>
            <th></th>
        </tr>
    
        <% @tag.photos.each do |photo| %>
            <tr>
                <td><%= link_to image_tag(photo.file.url(:thumb)), photo_path(photo) %></td>
            </tr>
        <% end %>
    </table>
    
    <br />
    
<% end %>

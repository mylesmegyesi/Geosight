<h1>Listing temp_photos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photos.each do |temp_photo| %>
  <tr>
    <td><%= link_to 'Show', temp_photo %></td>
    <td><%= link_to 'Edit', edit_temp_photo_path(temp_photo) %></td>
    <td><%= link_to 'Destroy', temp_photo, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Temp photo', new_upload_path %>

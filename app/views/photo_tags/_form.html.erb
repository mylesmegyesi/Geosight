<%= form_for(@photo_tag) do |f| %>
  <% if @photo_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_tag.errors.count, "error") %> prohibited this photo_tag from being saved:</h2>

      <ul>
      <% @photo_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %><br />
    <%= f.text_field :photo_id %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
